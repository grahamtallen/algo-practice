const walk = (
    graph: WeightedAdjacencyList,
    curr: number,
    needle: number,
    seen: number[],
    path: number[],
): number[] => {};
export default function dfs(
    graph: WeightedAdjacencyList,
    source: number,
    needle: number,
): number[] | null {}
